/* Copy to js/config.js (which is gitignored) and fill in YOUR values.
   These two Supabase values are public-by-design (security comes from
   Row Level Security policies) — but keep js/config.js out of the repo
   anyway so each deployment owns its own configuration.
   Full setup: AUTH_AND_STORAGE_PLAN.md */

window.ED_CONFIG = {
  supabaseUrl: "https://YOUR-PROJECT.supabase.co",
  supabaseAnonKey: "YOUR-ANON-KEY"
};
